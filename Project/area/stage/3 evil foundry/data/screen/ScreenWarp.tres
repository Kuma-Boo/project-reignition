[gd_resource type="ShaderMaterial" load_steps=3 format=2]

[ext_resource path="res://area/stage/3 evil foundry/data/screen/screen warp.png" type="Texture" id=1]

[sub_resource type="Shader" id=1]
code = "shader_type canvas_item;

uniform sampler2D distortion : hint_albedo;
uniform float intensity = 128.0;
uniform vec2 scroll_speed = vec2(1, 1);
uniform vec2 scale = vec2(1, 1);

void fragment()
{
	vec4 warp = texture(distortion, UV * scale + scroll_speed * TIME);
	vec2 uv = SCREEN_UV + (vec2((warp.g * intensity) - intensity * .5, 0) * SCREEN_PIXEL_SIZE.x);
	vec4 col = texture(SCREEN_TEXTURE, uv);
	COLOR = col;
}"

[resource]
shader = SubResource( 1 )
shader_param/intensity = 128.0
shader_param/scroll_speed = Vector2( -0.4, 0.8 )
shader_param/scale = Vector2( 1.6, 3.2 )
shader_param/distortion = ExtResource( 1 )
